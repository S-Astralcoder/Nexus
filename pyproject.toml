[project]
name = "nexus"
version = "0.1.0"
description = "Agent building framework to simplify the building process by reducing redundant code."
readme = "README.md"
authors = [
    { name = "S-Astralcoder", email = "suraj.astralcode.ai@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.97.0",
]

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
